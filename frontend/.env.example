# Frontend dev server port
VITE_PORT=5173

# Dev proxy: where /api requests are forwarded (npm run dev only)
VITE_API_PROXY_TARGET=http://localhost:5000

# API base URL for axios (use /api with proxy in dev, or full URL in production)
VITE_API_BASE_URL=/api
